import { game } from "./game.js";

describe("Initial test", () => {
  test("Test", () => {
    /*
    Const list = 'a';
    const expectResult = 3;
    const result = game(list);
    expect(result).toBe(expectResult);
    */
  });
});
